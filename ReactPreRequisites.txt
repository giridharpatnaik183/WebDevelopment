React
-----
Node Installation (Npm installed by default with this) --used to load Js

Extensions required 
--------------------
Thunder Client - works as a postman for testing Api's to hit http request
ES7 React/Redux/GraphQL/React-Native snippets - auto suggests the tag's 
Bracket Pair Color DLW - maintains the brackets with colors
Auto Rename/Complete Tag - to add close tag and rename paired tag automatically for HTML/XML
Live Server - local Server with live reload feature for static & dynamic pages
Prettier -  is an opinionated code formatter. It enforces a consistent style by parsing your code and re-printing it with its own rules that take the maximum line length into account, wrapping code when necessary.




